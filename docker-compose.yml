services:
  app:
    build: .
    ports:
      - "${PORT:-17832}:8000"
    environment:
      APP_ENV: ${APP_ENV:-development}
      ANKI_URL: http://host.docker.internal:8765
    volumes:
      - ./data:/data
    extra_hosts:
      - "host.docker.internal:host-gateway"
