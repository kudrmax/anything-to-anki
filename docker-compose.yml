services:
  app:
    build: .
    ports:
      - "${PORT:-17832}:8000"
    environment:
      APP_ENV: ${APP_ENV:-development}
      ANKI_URL: http://host.docker.internal:8765
      AI_PROXY_URL: http://host.docker.internal:8766
    volumes:
      - ./data:/data
